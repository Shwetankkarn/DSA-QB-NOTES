class Solution {
    public boolean isPalindrome(int x) {

      if(x<0) return false;

      int rem=0, ans=0 ,original=x;

        while(x>0){
        
        rem= x%10;
       ans= ans*10+ rem;
       x=x/10;
        }

       if(ans==original) return true;

       else return false;
    }
}
